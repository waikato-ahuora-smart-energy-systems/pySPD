# Gate 12 checklist

| ID | Exit criterion | Current evidence | Status |
|---|---|---|---|
| `G12-01` | Exactly 546 unique affected identities across all 139 Gate 1 hashes | All 139 hashes screened; 434 analytic candidates remain diagnostic only. Five default-profile dates and the targeted 2022-11-24 recovery are complete. The qualified residue profile passed 2022-11-25 (five identities), 2022-12-04 (one), and 2023-01-16 (eight), without solver-option changes. Nine dates are complete; the remaining dates and final exact-546 manifest are pending | In progress |
| `G12-02` | All 546 cases replayed through pinned GAMS and PySPD | Incremental checkpoint feed, canonical prefix planner, streaming PySPD and pinned GAMS producers, twelve-surface bundles, exact numeric diff, semantic validator, independent price certificates, and report crosswalk are implemented. The post-WPT 2022-11-06 replay completed the full 196-case prefix in both engines. All 196 GAMS primary and fixed-RMIP pricing solves were optimal. The zero-flow-certified semantic profile has zero unresolved numeric differences and retains only four explicit `report-field` schema records. Remaining dates are pending population discovery and replay | In progress |
| `G12-03` | Complete normal, outage, high/negative-price, scarcity, islanding, 46-period, and 50-period days | Required categories enforced by `Gate12EvidenceIndex`; Gate 1 DST inputs are hash-bound | Pending execution |
| `G12-04` | Identical selection, order, prior-dispatch initialization, and fallback path | Historical checkpoints require exact canonical GDX progress order; application selection fails on missing/duplicate identities; first paired date passes exact case-selection, publication-seconds, and state-transition surfaces for all four affected cases | Partial |
| `G12-05` | Primary physics, objective, and fixed-discrete pricing state parity | The post-WPT first paired date passes accepted fixed-RMIP physics, fixed-RMIP objective (maximum error `3.23e-11 NZD`), and fixed-discrete state under the named semantic policy. Raw SCIP MIP objective is retained as qualified diagnostic evidence. Portable v16 representative objective and independent validation also pass | Partial |
| `G12-06` | Raw/repaired/node/reserve/published price parity or valid case-specific certificate | The post-fix full-prefix certificate passes raw/repaired buses 816/820 and node `WPT1101` within `1.8e-14 NZD/MWh`; fixed-RMIP objective error is `3.23e-11 NZD`. The independent source-topology certificate proves 58 load-side leaf-bus derivatives, projects the material `KIN1009` node delta with zero residual, and exactly reconstructs three affected five-decimal publications from their six weighted cases. The 2022-11-06 v5 price criterion now passes under the documented convention. Portable v16 reserve certificate passes; 389 v16 node prices remain unresolved | Partial |
| `G12-07` | Every in-scope report identity and field compared | The zero-flow-certified mapped-row artifact compares 13,133 values with zero missing or extra identities, classifies 57 convention-bound differences, and leaves zero values above Authority precision. Eight risk/summary tables remain unimplemented. The schema crosswalk records 77 unsupported Authority fields per case, 12 unmatched candidate fields per case, and candidate-only `audit` | In progress |
| `G12-08` | Repeat and checkpoint/resume equality | Hash-qualified atomic population resume implemented; whole-day repeat/resume output equality mandatory | Partial |
| `G12-09` | SCIP-MIP → fix discrete → HiGHS-RMIP portable profile passes | Application configuration explicitly selects and hashes `scip-mip-fixed-highs-rmip`; representative v5/v16 pathways and independent validators pass. The full 196-case 2022-11-06 governed prefix completed under `pyspd-v5-portable-scip-mip-fixed-highs-rmip-v1`; complete population execution remains pending | Partial |
| `G12-10` | Strict historical CPLEX profile executed | Installed command-line GAMS licence does not entitle CPLEX; GAMSPy-only licence cannot execute the existing `$include`-based GAMS source | Blocked on compatible CPLEX execution entitlement |
| `G12-11` | Source, configuration, code, dependency, solver, result, comparison, and report hashes indexed | Fail-closed index, exact twelve-surface case artifact hasher, canonical PySPD twelve-surface exporter, separate original/repeat/resume day hashes, and per-day raw-artifact hash contract implemented; ordered selection and explicit solver profile flow into daily result/checkpoint/report provenance. Streaming checkpoints and final bundles bind the Python/runtime source fingerprint; GAMS bundles also bind the pinned source tree and executable bytes. The first completed candidate bundle is indexed in `pyspd-replay-20221106.json`, including source, work-item, configuration, execution, checkpoint, and result hashes | Pending final evidence |
| `G12-12` | Zero unresolved material discrepancies | Deterministic identity-strict discrepancy register implemented; resolution requires paired text and evidence hash, and closure rejects every open material record | Pending execution |

Gate 12 remains open until every row is met. No separate independent human
reviewer or approval is required, but no pending, partial, or blocked row may be
treated as passing evidence.
