# Gate 12 checklist

| ID | Exit criterion | Current evidence | Status |
|---|---|---|---|
| `G12-01` | Exactly 546 unique affected identities across all 139 Gate 1 hashes | All 139 hashes screened; 434 analytic candidates remain diagnostic only. The daily-mode exact-positive selector is invalidated because three dates emitted 737 false memberships. The non-daily, first-loop, actual material-transfer method passed canonical qualification on 2022-11-06: 270/270 optimal cases and exactly four identities/six node transfers matching the independent reconstruction. Five default-profile dates are complete and qualifying. The next date stalls after a SCIP optimum on a GAMS-rejected original-row residual. Global `1e-9`/`1e-10` calibrations still produced rejected residuals and no checkpoints. A separately hash-addressed 2022-11-24 shard now tightens only case `241012022111000704`; it remains nonqualifying until the complete date passes | In progress |
| `G12-02` | All 546 cases replayed through pinned GAMS and PySPD | Incremental checkpoint feed, canonical prefix planner, durable case-streaming PySPD producer, pinned daily-mode GAMS instrumentation/export producer, twelve-surface bundle store, exact per-date parity checkpoint coordinator, immutable path-level numeric diff, semantic result, independent bus-degeneracy certificate, and hash-bound report-schema crosswalk implemented. The paired 2022-11-06 prefix completed all 196 cases and compared four affected cases. Exact comparison changed 36/48 surfaces; the certified semantic policy reduced this to five unresolved paths across five surfaces. Remaining dates are pending population discovery and replay | In progress |
| `G12-03` | Complete normal, outage, high/negative-price, scarcity, islanding, 46-period, and 50-period days | Required categories enforced by `Gate12EvidenceIndex`; Gate 1 DST inputs are hash-bound | Pending execution |
| `G12-04` | Identical selection, order, prior-dispatch initialization, and fallback path | Historical checkpoints require exact canonical GDX progress order; application selection fails on missing/duplicate identities; first paired date passes exact case-selection, publication-seconds, and state-transition surfaces for all four affected cases | Partial |
| `G12-05` | Primary physics, objective, and fixed-discrete pricing state parity | First paired date passes accepted fixed-RMIP physics, fixed-RMIP objective (maximum error `1.36e-11 NZD`), and fixed-discrete state under the named semantic policy. Raw SCIP MIP objective is retained as qualified diagnostic evidence. Portable v16 representative objective and independent validation also pass | Partial |
| `G12-06` | Raw/repaired/node/reserve/published price parity or valid case-specific certificate | First paired date passes node and reserve prices. The source-matrix certificate proves all above-tolerance bus deltas project to at most `9.592326932761353e-14 NZD/MWh` at nodes and passes all four cases. One `TP35/WPT1101` published energy price remains different by `0.00101 NZD/MWh`. A bounded optimal no-fallback alternative runner is implemented, but its first formal result (`13.18042`) also fails against GAMS (`13.18203`); a prior diagnostic run at `13.18202` establishes variability but is not selected as a pass. Portable v16 reserve certificate passes; 389 v16 node prices remain unresolved | Open |
| `G12-07` | Every in-scope report identity and field compared | The first paired date now has a bundle-hash-bound schema crosswalk and mapped-row projector over all four affected cases. Per case, all 13 Authority tables/142 fields are classified: 65 fields map directly, through a declared derivation, or through a wide-to-long pivot; 77 Authority fields remain unsupported, 12 candidate-field occurrences remain unmatched, and `audit` is candidate-only. The v1 row projector compared 12,619 mapped values: all 2,092 node prices, 395 offer quantities, 16 reserve-result prices, 16 island reserve prices, and 16 published reserve prices pass; 2,091/2,092 published energy prices pass. Remaining pre-fix evidence is 166 branch identities, 22 branch-flow precision values, 11 bus prices, one published energy price, and four unimplemented table projections per case. V2 adds exact-identity LHS/RHS/sense projection for 348 constraint observables. HVDC values, report-only inactive-branch zeros, and fixed-RMIP report extraction are fixed in code but require replay | In progress |
| `G12-08` | Repeat and checkpoint/resume equality | Hash-qualified atomic population resume implemented; whole-day repeat/resume output equality mandatory | Partial |
| `G12-09` | SCIP-MIP → fix discrete → HiGHS-RMIP portable profile passes | Application configuration explicitly selects and hashes `scip-mip-fixed-highs-rmip`; representative v5/v16 pathways and independent validators pass. The full 196-case 2022-11-06 governed prefix completed under `pyspd-v5-portable-scip-mip-fixed-highs-rmip-v1`; complete population execution remains pending | Partial |
| `G12-10` | Strict historical CPLEX profile executed | Installed command-line GAMS licence does not entitle CPLEX; GAMSPy-only licence cannot execute the existing `$include`-based GAMS source | Blocked on compatible CPLEX execution entitlement |
| `G12-11` | Source, configuration, code, dependency, solver, result, comparison, and report hashes indexed | Fail-closed index, exact twelve-surface case artifact hasher, canonical PySPD twelve-surface exporter, separate original/repeat/resume day hashes, and per-day raw-artifact hash contract implemented; ordered selection and explicit solver profile flow into daily result/checkpoint/report provenance. Streaming checkpoints and final bundles bind the Python/runtime source fingerprint; GAMS bundles also bind the pinned source tree and executable bytes. The first completed candidate bundle is indexed in `pyspd-replay-20221106.json`, including source, work-item, configuration, execution, checkpoint, and result hashes | Pending final evidence |
| `G12-12` | Zero unresolved material discrepancies | Deterministic identity-strict discrepancy register implemented; resolution requires paired text and evidence hash, and closure rejects every open material record | Pending execution |

Gate 12 remains open until every row is met. No separate independent human
reviewer or approval is required, but no pending, partial, or blocked row may be
treated as passing evidence.
