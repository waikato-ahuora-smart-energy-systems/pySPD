# Gate 12 checklist

| ID | Exit criterion | Current evidence | Status |
|---|---|---|---|
| `G12-01` | Exactly 546 unique affected identities across all 139 Gate 1 hashes | All 139 hashes screened; 434 analytic candidates remain diagnostic only. Five default-profile dates and the targeted 2022-11-24 recovery are complete. The qualified residue profile passed 2022-11-25 (five identities), 2022-12-04 (one), 2023-01-16 (eight), and 2023-01-17 (one), without solver-option changes. Ten dates are complete; the remaining dates and final exact-546 manifest are pending | In progress |
| `G12-02` | All 546 cases replayed through pinned GAMS and PySPD | Incremental checkpoint feed, canonical prefix planner, streaming PySPD and pinned GAMS producers, twelve-surface bundles, exact numeric diff, semantic validator, independent price certificates, and report crosswalk are implemented. The complete paired prefixes for 2022-11-06 (196 cases/four affected) and 2023-01-17 (191 cases/one affected) passed in both engines. All primary and fixed-RMIP pricing solves were optimal; the report-certified semantic profile passes all five affected cases and twelve surfaces with zero unresolved differences. Remaining dates are pending population discovery and replay | In progress |
| `G12-03` | Complete normal, outage, high/negative-price, scarcity, islanding, 46-period, and 50-period days | Required categories enforced by `Gate12EvidenceIndex`; Gate 1 DST inputs are hash-bound | Pending execution |
| `G12-04` | Identical selection, order, prior-dispatch initialization, and fallback path | Historical checkpoints require exact canonical GDX progress order; application selection fails on missing/duplicate identities. Both paired dates pass exact case-selection, publication-seconds, and state-transition surfaces across all five affected cases | Partial |
| `G12-05` | Primary physics, objective, and fixed-discrete pricing state parity | Both paired dates pass accepted fixed-RMIP physics, fixed-RMIP objective, and fixed-discrete state under the named semantic policy. The first-date maximum fixed-RMIP objective error is `3.23e-11 NZD`; the second-date maximum is about `5.35e-5 NZD`, within the `1e-4` governed tolerance. Raw SCIP MIP objective is retained as qualified diagnostic evidence. Portable v16 representative objective and independent validation also pass | Partial |
| `G12-06` | Raw/repaired/node/reserve/published price parity or valid case-specific certificate | The 2022-11-06 source-topology certificate proves 58 load-side leaf-bus derivatives, the material `KIN1009` projection, and three affected publications. On 2023-01-17 it proves 49 bus derivatives, projects `GOR2202`, `OTA2202`, and `WPT1101` with zero node residual, and reconstructs their TP34 publications within `0.00002 NZD/MWh`; the largest certified node difference is `0.11549769467476 NZD/MWh`. Both paired v5 price criteria pass. Portable v16 reserve certification passes; 389 v16 node prices remain unresolved | Partial |
| `G12-07` | Every in-scope report identity and field compared | Both paired dates map all Authority fields across all 13 tables. The final mapped-row artifacts compare 68,558 values on 2022-11-06 and 17,203 on 2023-01-17 with zero missing or extra identities, zero above-tolerance values, and zero unimplemented tables. Hash-bound report results are accepted by semantic parity with zero unresolved report records. Remaining governed dates are pending | Partial |
| `G12-08` | Repeat and checkpoint/resume equality | Hash-qualified atomic population resume implemented; whole-day repeat/resume output equality mandatory | Partial |
| `G12-09` | SCIP-MIP → fix discrete → HiGHS-RMIP portable profile passes | Application configuration explicitly selects and hashes `scip-mip-fixed-highs-rmip`; representative v5/v16 pathways and independent validators pass. The full governed prefixes for 2022-11-06 (196 cases) and 2023-01-17 (191 cases) completed under `pyspd-v5-portable-scip-mip-fixed-highs-rmip-v1`; complete population execution remains pending | Partial |
| `G12-10` | Strict historical CPLEX profile executed | Installed command-line GAMS licence does not entitle CPLEX; GAMSPy-only licence cannot execute the existing `$include`-based GAMS source | Blocked on compatible CPLEX execution entitlement |
| `G12-11` | Source, configuration, code, dependency, solver, result, comparison, and report hashes indexed | Fail-closed index, exact twelve-surface case artifact hasher, canonical PySPD twelve-surface exporter, separate original/repeat/resume day hashes, and per-day raw-artifact hash contract implemented; ordered selection and explicit solver profile flow into daily result/checkpoint/report provenance. Streaming checkpoints and final bundles bind the Python/runtime source fingerprint; GAMS bundles also bind the pinned source tree and executable bytes. The two completed paired dates are indexed in `report-completeness-20221106.json` and `replay-certification-20230117.json`; remaining dates and the final consolidated index are pending | Pending final evidence |
| `G12-12` | Zero unresolved material discrepancies | Deterministic identity-strict discrepancy register implemented; resolution requires paired text and evidence hash, and closure rejects every open material record | Pending execution |

Gate 12 remains open until every row is met. No separate independent human
reviewer or approval is required, but no pending, partial, or blocked row may be
treated as passing evidence.
